import * as React from 'react';
declare type TProps = {
    children: React.ReactNode;
    height: number;
    width: number;
};
export default function EdgesContainer(props: TProps): JSX.Element;
export {};
